---
name: market-event-impact
description: FOMC·CPI 같은 정례 발표부터 은행 파산·신용등급 변경·관세·지정학 충격까지, 특정 사건이 무엇을 바꿨고 시장이 왜 반응했는지 공식자료·당시 보도·확정 가격으로 분석하는 한국어 대화형 스킬. 사건 시각과 자산별 관측시각을 맞춰 금리·외환·주식 전달경로를 검증하고, 이벤트 스터디를 보조 증거로 제시한다. Use for named market-moving events. Not for 실시간 호가·예측·매매 추천.
license: MIT
metadata:
  category: finance
  locale: ko-KR
  phase: v1
---

# market-event-impact

<!-- k-skill:cli-stub — generated by scripts/generate-skill-stubs.js; edit skill.json / instruction.md instead -->

## Get the full instructions (required first step)

Run this and follow its output as the primary instructions for this skill:

```bash
npx -y @nomadamas/k-skill@0 instruct market-event-impact
```

The CLI detects the current runtime (Dolshoi vault/CloakBrowser vs generic) and prints only the applicable instructions, always up to date. Helper files bundled with the CLI are listed by:

```bash
npx -y @nomadamas/k-skill@0 files market-event-impact
```

Keep the CLI and every coding-agent skill install current (including Vercel Agent Skills) with:

```bash
npx -y @nomadamas/k-skill@0 update
```

If `npx` is unavailable, install Node.js 18+ or follow https://github.com/NomaDamas/k-skill#readme, or read the source instructions at https://github.com/NomaDamas/k-skill/blob/main/market-event-impact/instruction.md.

## Hard rules even without the CLI

- Never execute payment, message/email delivery, final submission, cancellation, or public posting without the user's explicit approval immediately beforehand.
- Never ask for, print, or store plaintext credentials in chat, files, or shell arguments.
- Never bypass legal, physical-presence, CAPTCHA, identity-proofing, or electronic-signature boundaries.
